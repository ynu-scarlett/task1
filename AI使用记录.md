# AI 使用记录

- 日期：2026-09-24
- 工作目录：`d:\桌面\task1`
- AI 工具：Trae 编程助手
- 涉及文件：`compound_interest.py`、`index.html`、`experiment`、本记录文件
- 验证环境：Windows + PowerShell，`python` 命令直接运行脚本（设置 `PYTHONIOENCODING=utf-8` 保证中文输出正常）；网页用本地 `python -m http.server 8000` + 浏览器自动化检查

> 说明：以下"指令原文"为用户逐条发出的原话（仅统一了标点显示），"验证过程"为 AI 实际执行的检查动作与客观结果，未做事后修饰。

---

## 指令 1（Step 1：基础复利程序）

**指令原文：**
> 现在呢，要进行step1，要用Python 写一个复利计算程序： 本金 10000 元，年利率 5%，存 3 年，每年复利 1 次；要 输出最终本息和，保留两位小数；还要记得 代码要带注释，逐行解释公式含义

**处理：** 新建 `compound_interest.py`，固定参数 P=10000、r=0.05、n=1、t=3，公式 `A = P × (1 + r/n)**(n×t)`，逐行中文注释，`:.2f` 保留两位小数。

**验证过程：**
- 执行 `python compound_interest.py`
- 实际输出：`最终本息和为：11576.25 元`
- 手工复算：10000 × 1.05³ = 10000 × 1.157625 = 11576.25 ✓

---

## 指令 2（美观网页）

**指令原文：**
> 还要有一个好看的页面

**处理：** 新建单文件 `index.html`（CSS/JS 全部内联，零外部依赖）：左侧参数表单（本金/利率/年限/复利频率，默认 10000、5%、3 年、年复利），右侧渐变结果卡、三指标统计、纯 SVG 堆叠柱状图、逐年明细表，输入即实时重算。

**验证过程：**
- 后台启动 `python -m http.server 8000`，HTTP 探活返回 200
- 浏览器自动化检查并截图：默认参数显示 **11,576.25**、总利息 1,576.25、比例 15.76%；明细表三行 10,500.00 / 11,025.00 / 11,576.25 ✓
- 交互测试：利率改为 10，结果实时变为 **13,310.00** ✓
- 发现并修复 1 个问题：SVG 用 `preserveAspectRatio="none"` 导致柱顶文字横向拉伸；改为按容器实际像素宽度 1:1 渲染并加窗口 resize 防抖重绘，复检文字清晰不变形 ✓

---

## 指令 3（Step 2：交互式 + 负利率拦截）

**指令原文：**
> 然后进行step2，在上一步基础上改成交互式，在用户输入本金/利率/年限时，程序输出结果；然后输入负数利率时提示"利率不能为负"并要求重输

**处理：** 改为 `input()` 循环读取本金/利率/年限；`try/except ValueError` 拦截非数字；本金、年限校验 >0；利率为负时输出指定提示语"利率不能为负，请重新输入！"。

**验证过程（管道喂入多行输入）：**
- 正常：`10000 / 5 / 3` → 11576.25 ✓
- 负利率：`10000 / -2 / 5 / 3` → 打印"利率不能为负，请重新输入！"后回到利率输入，最终 11576.25 ✓
- 混合非法：`abc / 0 / 10000 / -3 / 5% / 3` → 非数字、本金 0 均被拦截重输，最终结果正确 ✓

---

## 指令 4（Step 2 复核：0.05 与 -0.05）

**指令原文：**
> 然后我要你检查一下，当输入 10000 / 0.05 / 3 仍得 11576.25；然后输入 -0.05 被正确拦截

**验证过程与发现的问题：**
- 首轮实测：输入 `0.05` 得到 **10015.01**，不符合预期。根因：Step 2 把利率按"百分数"解释，0.05 被当成 0.05%（0.0005）。
- 修复：利率改为按**小数**解释（0.05 = 5%），同时兼容带 `%` 的写法（5% → 0.05）；`-0.05` 仍走负数拦截。
- 修复后复测：
  - `10000 / 0.05 / 3` → **11576.25** ✓
  - `10000 / -0.05 → 0.05 / 3` → 提示"利率不能为负，请重新输入！"，重输后 11576.25 ✓
  - `10000 / -5% → 5% / 3` → 同样被拦截，最终 11576.25 ✓
  - `10000 / abc → 0.05 / 3` → "输入无效，请输入数字！"，重输后正常 ✓

---

## 指令 5（Step 3：复利 vs 单利对比表）

**指令原文：**
> 然后Step3，现在要把他扩展为复利 vs 单利对比表：然后年限 10/20/30 年三行，列输出"复利终值/单利终值/差额"

**处理：** 保留交互输入本金、利率（含全部校验），不再问年限；用 `P*(1+r)**t`（复利）与 `P*(1+r*t)`（单利）输出 10/20/30 三行对比，差额=复利−单利，千分位两位小数，手工按中文双宽度补齐表头空格。

**验证过程：**
- `10000 / 0.05` 输出：
  - 第10年 16,288.95 / 15,000.00 / 1,288.95
  - 第20年 26,532.98 / 20,000.00 / 6,532.98
  - 第30年 43,219.42 / 25,000.00 / 18,219.42 ✓
- 非法输入复测：本金 `-5000`、利率 `-0.05` 均被拦截并重输，表格随后正常输出 ✓
- 发现表头第二、三列因中文双宽度比数据列多 1 个空格，已删除多余空格使竖线对齐 ✓

---

## 指令 6（Step 3 复核：30 年理论值）

**指令原文：**
> 然后呢，现在要检查Step3，有没有达到要求，30 年差额应约等于 10000×(1.05³⁰ − 1 − 0.05×30) ≈ 18219 元，然后复利终值 43219.42 vs 单利 25000

**验证过程（独立公式交叉核算，不复用程序自身代码）：**
- 另起 `python -c` 直接计算 `P*(1+r)**t` 与 `P*(1+r*t)`：
  - 30 年复利 = 10000×1.05³⁰ = **43219.42**
  - 30 年单利 = 10000×(1+0.05×30) = **25000.00**
  - 30 年差额独立公式 = 10000×(1.05³⁰−1−0.05×30) = **18219.42**
- 与程序表格输出逐项完全一致；10 年、20 年两档同步核对一致 ✓
- 结论：Step 3 达标，未改代码。

---

## 指令 7（experiment 文件：独立复利计算器）

**指令原文：**
> 现在在这个（`d:\桌面\task1/experiment`）文件里面，忽略我上面所有的要求，写一个复利计算器

**处理：** 在空文件 `experiment` 中从零编写独立计算器：本金/利率/年限交互输入，菜单选 5 种复利频率（年/半年/季/月/日），输出本息和、总利息、利息占比，整数年附逐年明细表。

**验证过程：**
- `10000 / 5 / 3 / 年复利` → **11,576.25**，明细 10,500.00 → 11,025.00 → 11,576.25 ✓
- `10000 / 5 / 10 / 月复利`（120 期）→ **16,470.09**，与 10000×(1+0.05/12)^120 一致 ✓
- 非法输入：abc、本金 0、负利率、年限 0、菜单选 9 全部被拦截并重输 ✓

---

## 指令 8（本次：Step2/3 扩展，四项基本要求）

**指令原文：**
> 然后现在要在这个（`d:\桌面\task1/compound_interest.py`）文件里面，在Step2/3基础上扩展，要达到以下这些基本要求
> 1 单利对比已含(Step3复核)
> 2 参数校验扩展:年限≤0、本金≤0也拦截
> 3 表格输出为对齐的字符串表格(不依赖外部库)
> 4 AI使用记录(每个指令原文+你的验证过程)

**处理：**
1. 单利对比：保留 10/20/30 年三行"复利终值/单利终值/差额"，口径与 Step3 复核一致（复利 `P*(1+r)**t`，单利 `P*(1+r*t)`）。
2. 参数校验：统一为 `read_number`（非数字重输）+ `read_until_valid`（业务规则）两级；本金 >0、利率 ≥0（负数提示"利率不能为负，请重新输入！"）、年限 >0，≤0 全部拦截；利率兼容 0.05 与 5% 两种写法。
3. 对齐表格：新增 `print_table`，按字符 Unicode 区间计算中文/全角字符的 2 格显示宽度，自动求每列最大宽度并补空格对齐；仅用 Python 内置能力，无任何第三方库。
4. AI 使用记录：即本文件。

**验证过程：**
- 正常输入 `10000 / 0.05 / 3`：
  - 单条结果"存满 3 年后：本息和 11,576.25 元（利息 1,576.25 元）" ✓
  - 对比表三行：16,288.95/15,000.00/1,288.95；26,532.98/20,000.00/6,532.98；43,219.42/25,000.00/18,219.42 ✓
  - 表头与数据竖线在终端中等宽对齐（中文双宽度已计入）✓
- 非法输入组合 `本金: abc→0→-8000→10000；利率: -0.05→5%；年限: 0→-3→xyz→3`：
  - abc → "输入无效，请输入数字！"
  - 本金 0、-8000 → "本金必须大于 0，请重新输入！"（本金 ≤0 拦截）✓
  - 利率 -0.05 → "利率不能为负，请重新输入！"；5% 正确换算为 5% ✓
  - 年限 0、-3 → "年限必须大于 0，请重新输入！"（年限 ≤0 拦截）；xyz → 非数字拦截 ✓
  - 全部重输为合法值后输出 11,576.25 与正确对比表 ✓
- 依赖检查：脚本仅使用内置函数（`input/print/float/ord` 等），无 `import` 第三方库，满足"不依赖外部库" ✓

---

## 汇总：各步骤关键数值对照

| 场景 | 预期/理论值 | 程序实际输出 | 结论 |
|---|---|---|---|
| 10000、5%、3 年、年复利 | 11,576.25 | 11,576.25 | ✓ |
| 10000、5%、10 年、月复利 | 16,470.09 | 16,470.09 | ✓ |
| 5%、10 年：复利/单利/差额 | 16288.95 / 15000 / 1288.95 | 一致 | ✓ |
| 5%、20 年：复利/单利/差额 | 26532.98 / 20000 / 6532.98 | 一致 | ✓ |
| 5%、30 年：复利/单利/差额 | 43219.42 / 25000 / 18219.42 | 一致 | ✓ |
| 本金 ≤0、年限 ≤0、利率 <0、非数字 | 拦截并重输 | 全部拦截 | ✓ |
